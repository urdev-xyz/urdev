module.exports = {
    siteUrl: `https://ur.dev`, // Site domain. Do not include a trailing slash!

    postsPerPage: 6, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `ur.dev`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Web and App Development Made Simple.`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `ur.dev`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#e9e9e9`, // Used for Offline Manifest
    themeColor: `#EDF2F7    `, // Used for Offline Manifest
}
